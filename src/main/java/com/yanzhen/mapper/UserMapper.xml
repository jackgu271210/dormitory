<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yanzhen.mapper.UserMapper">
    <insert id="create" keyProperty="id" useGeneratedKeys="true" parameterType="com.yanzhen.entity.User">
        insert into tb_user (user_name,password,name,phone,type,remark)
        values (#{userName},#{password},#{name},#{phone},#{type},#{remark})
    </insert>
    
    <delete id="delete">
        delete from tb_user where id = #{id}
    </delete>
    
    <update id="update">
        update tb_user set user_name = ?, password = ?, name = ?, phone = ?, type = ?, remark = ?
        where id = #{id}
    </update>
    
    <select id="query" resultMap="User">
        
    </select>
    
    <select id="detail">
        
    </select>
</mapper>